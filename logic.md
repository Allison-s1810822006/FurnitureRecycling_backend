# 🪑 申請邏輯說明（家具清運申請頁）

## 🧩 一、前端操作流程

### 1️⃣ 家具類型選擇
前端可選的**家具類型**共有兩大類：
- **沙發類**
    - 單人座沙發
    - 雙人座沙發
    - 三人座沙發
- **床架類**
    - 單人床架
    - 雙人床架

---

### 2️⃣ 選擇邏輯
1. 使用者先選擇「家具類型」（下拉式選單）。
2. 下方會出現該類型的**細分類選項**。
3. 每個細分類選項右側有「張數控制器」（初始值為 0，可用 ➕➖ 調整）。
4. 使用者一旦對某細分類設定了大於 0 的數量，**該類型的其他選項將鎖定不可再選**。
    - 若要切換其他類型或選項，需先將目前的選項與照片**確認添加**至「已選家具區塊」。

---

### 3️⃣ 照片上傳與添加流程
1. 在設定數量後，需上傳相同數量的照片。
2. 點擊「添加到已選家具」按鈕，即可把選項與照片加入下方的「已添加項目區塊」。
3. 「已添加項目區塊」會顯示：
    - 細分類型名稱
    - 數量
    - 照片縮圖

---

### 4️⃣ 送出前可編輯
在送出申請前：
- 可**刪除項目**
- 可**修改數量**
- 可**重新上傳照片**

---

## 🧮 二、驗證規則（Validation Rules）

在申請送出前需通過以下檢查：

| 編號 | 驗證項目 | 說明 |
|------|-----------|------|
| 0️⃣ | 站點每日限額 | 每個站點每日最多可收取 5 件家具。進入申請頁後，選擇站點與日期會顯示剩餘可收件數。 |
| 1️⃣ | 數量限制 | 每項家具數量不可小於 0、不可大於 5。 |
| 2️⃣ | 至少一項 | 送出申請前，必須至少有一個「已添加項目」。 |
| 3️⃣ | 照片數量 | 上傳照片數量必須與家具數量一致。 |
| 4️⃣ | 照片必備 | 每個已添加項目都需上傳至少一張照片。 |
| 5️⃣ | 照片格式 | 僅接受 jpg、png 檔，且大小不得超過 5MB。 |
| 6️⃣ | 類型選擇限制 | 一次僅能選擇一個家具類型，且僅能選該類型中的一個細分項目。 |
| 7️⃣ | 資料完整性 | 送出申請時，所有項目之數量與照片皆須符合上述規範。 |
| 8️⃣ | 錯誤提示 | 若違反規範，系統需顯示對應錯誤訊息並阻止送出。 |

---


# 🗂️ 新增application_items 資料表說明

## 📘 目的 Purpose
這張表用來記錄 **使用者清運申請單 (applications)** 中的家具細項內容，  
是 `applications` 與 `furniture_items` 之間的中介表。

主要用途：
- 紀錄每張申請單中包含的家具種類、數量與照片
- 支援多筆家具清單與上傳圖片功能
- 方便後台統計、排程與分析

簡單來說：
> 一張申請單 (Application) 可以包含多個家具項目 (ApplicationItem)，  
> 每個家具項目都對應一個家具類型 (FurnitureItem)。

---

## 🧩 表結構 Table Structure

| 欄位名稱 | 型別 | 說明 | 約束 |
|-----------|------|------|------|
| `item_id` | UUID | 主鍵，使用 UUID 產生 | PRIMARY KEY |
| `application_id` | UUID | 對應申請單 ID | 外鍵 → `applications.application_id` |
| `furniture_item_id` | UUID | 表示這個申請項目是哪一種家具 | 外鍵 → `furniture_items.furniture_item_id` |
| `item_name` | TEXT | 家具細分名稱 (例：單人沙發、雙人床架) | NOT NULL |
| `quantity` | INT | 家具數量 | 預設 0，不可為 NULL |
| `photos` | TEXT[] | 家具照片（圖片 URL 陣列） | 可為 NULL |
| `created_at` | TIMESTAMPTZ | 建立時間（含時區） | 預設 NOW() |
| `updated_at` | TIMESTAMPTZ | 更新時間（含時區） | 自動更新 NOW() |

---

## 🧠 關聯關係 Relationships

```text
Application 1 ────< ApplicationItem >──── 1 FurnitureItem


