# Furniture Recycling Backend 前台後端專案總覽  

## 專案簡介
本專案為「家具回收申請」前台的後端服務，支援 LINE OAuth2 綁定登入、會員管理、家具清運申請、資料驗證、申請流程控管等功能。資料庫採 PostgreSQL，API 以 Spring Boot 實作，結構清晰、驗證嚴謹，便於前後端協作。

---

## 主要功能架構

### 1. LINE 綁定登入／註冊
- 支援 LINE OAuth2 登入，判斷新舊會員。
- 新會員自動帶入 LINE 資料（頭像、姓名、電話、Email），前端補齊缺漏欄位。
- 註冊後可選「加入會員」或「下一步」直接進入申請流程。
- 已綁定者快速登入，直接進入申請頁。
- 會員資料表（app_users）完整記錄 LINE 相關欄位與一般會員欄位。

### 2. 家具清運申請流程
- 申請單（applications）可關聯多個家具細項（application_items）。
- 家具細項支援類型、細分選項、數量、照片（多張 URL）、時間戳。
- 申請流程支援前端動態編輯、項目刪除、數量修改、照片重傳。

### 3. 資料驗證規則
- 站點每日限額：每站每日最多 5 件家具。
- 數量限制：每項家具數量 1~5。
- 至少一項：送出申請前必須有一個項目。
- 照片數量：每項照片數量需等於家具數量。
- 照片格式：僅接受 jpg/png，大小 <= 5MB（副檔名後端驗證，大小前端驗證）。
- 類型選擇限制：一次僅能選一個家具類型及細分項目。
- 資料完整性：所有項目皆須符合規範。
- 錯誤提示：違規時回傳明確錯誤訊息。

---

## 資料表設計（PostgreSQL）

### app_users
- user_id (UUID, PK)
- full_name
- is_admin
- email
- phone
- line_user_id (唯一)
- line_display_name
- line_picture_url
- line_bound_at
- line_email

### applications
- application_id (UUID, PK)
- user_id (FK)
- station_id (FK)
- requested_date
- status
- created_at / updated_at
- total_items / total_volume_m3
- suggested_vehicle
- schedule_id (FK)
- sequence_no

### application_items
- item_id (UUID, PK)
- application_id (FK)
- furniture_item_id (FK)
- item_name
- quantity
- photos (TEXT[])
- created_at / updated_at

### furniture
- item_id (PK)
- item_name
- main_type / sub_type
- 長寬高
- type
- application_id (FK)

---

## 主要程式邏輯

### LINE 登入/註冊
- LineAuthController：處理 LINE OAuth2 登入、callback、判斷新舊會員。
- AppUsersController：支援 LINE快速註冊 API，接收前端補齊資料，建立會員。
- AppUsersService：根據 RegistrationDTO 建立會員，記錄 LINE 綁定時間。
- RegistrationDTO：封裝所有會員欄位（LINE userId、displayName、email、phone、fullName、pictureUrl）。

### 家具申請流程
- ApplicationController：申請單新增、查詢、更新、刪除。
- ApplicationItemController：家具細項新增、查詢、更新、刪除。
- ApplicationService：申請時執行所有驗證規則，確保資料正確。
- ApplicationRequestDto / ApplicationItemDto：封裝申請單與家具細項資料。

### 驗證規則（Service 層）
- 申請時檢查站點每日限額、數量、照片數量/格式、類型選擇、資料完整性。
- 驗證失敗時回傳明確錯誤訊息。

---

## 申請流程簡述
1. 使用者登入（LINE OAuth2），新會員補齊資料並註冊。
2. 進入申請頁，選擇家具類型、細分項目、數量，上傳照片。
3. 送出申請前，前端/後端共同驗證所有規則。
4. 申請成功後，資料寫入 applications/application_items。
5. 後台可查詢、管理所有申請與家具細項。

---

## 專案特色
- 完整支援 LINE OAuth2 綁定登入，會員資料自動帶入。
- 家具申請流程彈性，支援多項目、多照片、動態編輯。
- 嚴謹資料驗證，確保申請流程合規。
- 資料表設計清晰，易於擴充與維護。
- API 結構分明，便於前後端協作。

---

## 參考文件
- [logic.md]：業務流程與驗證規則說明
- [database.md]：資料表設計

---

